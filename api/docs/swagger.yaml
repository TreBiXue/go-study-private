info:
  contact: {}
paths:
  /GetCenterInfo/:
    get:
      consumes:
      - application/x-json-stream
      description: aaaaaa
      parameters:
      - description: 必須ではありません。
        in: query
        name: none
        type: string
      responses:
        "200":
          description: ok
          schema:
            type: string
swagger: "2.0"
